---
const services = [
  {
    icon: 'fas fa-globe',
    title: 'Diseño Web',
    description: 'Sitios web modernos, responsivos y atractivos. Optimizados para SEO y con efectos de hover y 3D cautivadores.',
    features: ['Diseño responsivo', 'Animaciones', 'SEO Performance', 'Velocidad máxima', 'Sistema de creación propio'],
    duration: '2-4 días aprox.',
    popular: true
  },
  {
    icon: 'fas fa-bullseye',
    title: 'Diseño de Anuncios',
    description: 'Creatividades publicitarias que captan la atención y maximizan las visitas en todas las plataformas.',
    features: ['Programas de Hollywood', 'Manipulación'],
    duration: '1-2 días aprox',
    popular: true
  },
  {
    icon: 'fas fa-bolt',
    title: 'SaaS (Software as a Service)',
    description: 'Desarrollo completo de plataforma de SaaS con sistemas y tecnologías de última generación.',
    features: ['Full Stack', 'Dashboard Admin', 'Agentic Development', 'Free Hosting and Database'],
    duration: '1 semana a 2 meses aprox.',
    popular: false
  },
  {
    icon: 'fas fa-palette',
    title: 'Branding Digital',
    description: 'Identidad visual completa para tu marca digital con todos los assets necesarios.',
    features: ['Logo Design', 'Brand Guidelines', 'Assets Digitales', 'Style Guide'],
    duration: '1-4 días aprox',
    popular: false
  }
];
---

<section class="design-services" id="servicios">
  <div class="services-container">
    <div class="section-header" data-gsap="fadeInUp">
      <span class="section-badge"><i class="fas fa-briefcase"></i> Nuestros Servicios</span>
      <h2 class="section-title">
        Servicios de diseño que
        <span class="gradient-text">impulsan</span>
      </h2>
      <p class="section-subtitle">
       Soluciones variadas y a medida. Con trato excelente y velocidad inigualable
      </p>
    </div>

    <div class="services-grid">
      {services.map((service, index) => (
        <div
          class={`service-card ${service.popular ? 'popular' : ''}`}
          data-gsap="slideInUp"
          data-delay={index * 0.1}
          data-hover-3d
        >
          {service.popular && (
            <div class="popular-badge">
              <span><i class="fas fa-star"></i> Más Popular</span>
            </div>
          )}

          <div class="card-header">
            <div class="service-icon"><i class={service.icon}></i></div>
            <div class="service-meta">
              <h3 class="service-title">{service.title}</h3>
            </div>
          </div>

          <p class="service-description">{service.description}</p>

          <div class="service-features">
            <h4>Incluye:</h4>
            <ul>
              {service.features.map(feature => (
                <li>
                  <span class="feature-check"><i class="fas fa-check"></i></span>
                  {feature}
                </li>
              ))}
            </ul>
          </div>

          <div class="service-footer">
            <div class="service-duration">
              <span class="duration-icon"><i class="fas fa-clock"></i></span>
              <span>{service.duration}</span>
            </div>
          </div>

          <div class="card-glow"></div>
        </div>
      ))}
    </div>

    <div class="services-note" data-gsap="fadeInUp">
      <div class="note-content">
        <span class="note-icon"><i class="fas fa-lightbulb"></i></span>
        <div class="note-text">
          <h4>¿Necesitas algo personalizado?</h4>
          <p>Todos nuestros servicios son completamente customizables. Utilizamos las últimas tecnologías en desarrollo. Hablemos</p>
        </div>
      </div>
    </div>
  </div>

  <div class="floating-elements">
    <div class="float-element float-1">
      <div class="element-content"><i class="fas fa-palette"></i></div>
    </div>
    <div class="float-element float-2">
      <div class="element-content"><i class="fas fa-sparkles"></i></div>
    </div>
    <div class="float-element float-3">
      <div class="element-content"><i class="fas fa-rocket"></i></div>
    </div>
  </div>
</section>

<style>
  .design-services {
    background: #000000;
    padding: clamp(5rem, 10vw, 8rem) 0;
    position: relative;
    overflow: hidden;
  }

  .services-container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 clamp(1rem, 5vw, 2rem);
    position: relative;
    z-index: 2;
  }

  .section-header {
    text-align: center;
    margin-bottom: clamp(4rem, 8vw, 6rem);
    opacity: 0;
    transform: translateY(30px);
  }

  .section-badge {
    display: inline-block;
    background: rgba(59, 180, 252, 0.15);
    color: #3bb4fc;
    padding: 0.5rem 1.2rem;
    border-radius: 50px;
    font-size: 0.9rem;
    font-weight: 500;
    margin-bottom: 1.5rem;
    border: 1px solid rgba(59, 180, 252, 0.3);
  }

  .section-title {
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    font-weight: 700;
    color: #ffffff;
    margin-bottom: 1.5rem;
    line-height: 1.2;
  }

  .gradient-text {
    background: linear-gradient(135deg, #3bb4fc, #ff6b9d);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .section-subtitle {
    font-size: clamp(1.1rem, 2.2vw, 1.3rem);
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.8);
    max-width: 800px;
    margin: 0 auto;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 2rem;
    margin-bottom: 4rem;
  }

  .service-card {
    position: relative;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 20px;
    padding: 2rem;
    transition: all 0.5s ease;
    opacity: 0;
    transform: translateY(30px);
    overflow: hidden;
    backdrop-filter: blur(10px);
  }

  .service-card.popular {
    border-color: rgba(59, 180, 252, 0.5);
    background: rgba(59, 180, 252, 0.08);
  }

  .service-card:hover {
    transform: translateY(-10px);
    background: rgba(255, 255, 255, 0.08);
    border-color: rgba(59, 180, 252, 0.3);
  }

  .popular-badge {
    position: absolute;
    top: -1px;
    right: 20px;
    background: linear-gradient(135deg, #3bb4fc, #2a9de8);
    color: #ffffff;
    padding: 0.4rem 1rem;
    border-radius: 0 0 15px 15px;
    font-size: 0.8rem;
    font-weight: 600;
    box-shadow: 0 5px 15px rgba(59, 180, 252, 0.3);
  }

  .card-header {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .service-icon {
    font-size: 3rem;
    line-height: 1;
    filter: drop-shadow(0 5px 15px rgba(59, 180, 252, 0.3));
  }

  .service-meta {
    flex: 1;
  }

  .service-title {
    font-size: 1.4rem;
    font-weight: 600;
    color: #ffffff;
    margin-bottom: 0.5rem;
  }

  .service-price {
    font-size: 1.1rem;
    font-weight: 600;
    color: #3bb4fc;
  }

  .service-description {
    color: rgba(255, 255, 255, 0.8);
    line-height: 1.6;
    margin-bottom: 1.5rem;
  }

  .service-features h4 {
    color: #ffffff;
    font-size: 0.9rem;
    font-weight: 600;
    margin-bottom: 1rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .service-features ul {
    list-style: none;
    padding: 0;
    margin: 0 0 2rem 0;
  }

  .service-features li {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.4rem 0;
    color: rgba(255, 255, 255, 0.8);
    font-size: 0.9rem;
  }

  .feature-check {
    color: #3bb4fc;
    font-weight: bold;
    font-size: 1rem;
  }

  .service-footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    margin-top: auto;
  }

  .service-duration {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: rgba(255, 255, 255, 0.7);
    font-size: 0.9rem;
  }

  .duration-icon {
    font-size: 1rem;
  }

  .service-cta {
    background: linear-gradient(135deg, #3bb4fc, #2a9de8);
    color: #ffffff;
    border: none;
    padding: 0.7rem 1.3rem;
    border-radius: 25px;
    font-weight: 600;
    font-size: 0.9rem;
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    box-shadow: 0 5px 15px rgba(59, 180, 252, 0.3);
  }

  .service-cta:hover {
    transform: translateY(-2px) scale(1.02);
    box-shadow: 0 8px 20px rgba(59, 180, 252, 0.4);
  }

  .cta-arrow {
    transition: transform 0.3s ease;
  }

  .service-cta:hover .cta-arrow {
    transform: translateX(3px);
  }

  .card-glow {
    position: absolute;
    inset: -2px;
    background: linear-gradient(45deg, transparent, rgba(59, 180, 252, 0.5), transparent);
    border-radius: 22px;
    opacity: 0;
    transition: opacity 0.3s ease;
    z-index: -1;
  }

  .service-card:hover .card-glow {
    opacity: 1;
  }

  .services-note {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 20px;
    padding: 2rem;
    backdrop-filter: blur(10px);
    opacity: 0;
    transform: translateY(20px);
  }

  .note-content {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    flex-wrap: wrap;
  }

  .note-icon {
    font-size: 2rem;
    filter: drop-shadow(0 0 10px rgba(255, 255, 255, 0.3));
  }

  .note-text {
    flex: 1;
    min-width: 250px;
  }

  .note-text h4 {
    color: #ffffff;
    font-size: 1.2rem;
    margin-bottom: 0.5rem;
  }

  .note-text p {
    color: rgba(255, 255, 255, 0.8);
    margin: 0;
  }

  .note-cta {
    background: transparent;
    color: #3bb4fc;
    border: 2px solid #3bb4fc;
    padding: 0.7rem 1.5rem;
    border-radius: 25px;
    text-decoration: none;
    font-weight: 600;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    transition: all 0.3s ease;
  }

  .note-cta:hover {
    background: #3bb4fc;
    color: #ffffff;
    text-decoration: none;
    transform: translateY(-2px);
  }

  .floating-elements {
    position: absolute;
    inset: 0;
    pointer-events: none;
    z-index: 1;
  }

  .float-element {
    position: absolute;
    width: 60px;
    height: 60px;
    background: rgba(59, 180, 252, 0.1);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    backdrop-filter: blur(10px);
    border: 1px solid rgba(59, 180, 252, 0.3);
  }

  .float-1 {
    top: 15%;
    left: 5%;
    animation: float 12s ease-in-out infinite;
  }

  .float-2 {
    top: 60%;
    right: 8%;
    animation: float 15s ease-in-out infinite reverse;
  }

  .float-3 {
    bottom: 20%;
    left: 15%;
    animation: float 18s ease-in-out infinite;
  }

  .element-content {
    font-size: 1.5rem;
  }

  @keyframes float {
    0%, 100% {
      transform: translateY(0px) rotate(0deg);
    }
    33% {
      transform: translateY(-20px) rotate(120deg);
    }
    66% {
      transform: translateY(10px) rotate(240deg);
    }
  }

  @media (max-width: 768px) {
    .services-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .section-header {
      opacity: 1;
      transform: translateY(0);
    }

    .service-card {
      padding: 1.5rem;
      opacity: 1;
      transform: translateY(0);
    }

    .services-note {
      opacity: 1;
      transform: translateY(0);
    }

    .card-header {
      flex-direction: column;
      text-align: center;
      gap: 0.5rem;
    }

    .service-footer {
      flex-direction: column;
      gap: 1rem;
    }

    .note-content {
      flex-direction: column;
      text-align: center;
      gap: 1rem;
    }
  }
</style>

<script>
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  if (typeof window !== 'undefined') {
    gsap.registerPlugin(ScrollTrigger);

    const initDesignServices = () => {
      const isMobile = window.innerWidth <= 768;

      if (!isMobile) {
        // Section header animation
        gsap.fromTo('[data-gsap="fadeInUp"]', {
          opacity: 0,
          y: 30
        }, {
          opacity: 1,
          y: 0,
          duration: 1,
          ease: "power2.out",
          scrollTrigger: {
            trigger: '[data-gsap="fadeInUp"]',
            start: "top 80%",
            toggleActions: "play none none reverse"
          }
        });

        // Service cards animation
        document.querySelectorAll('[data-gsap="slideInUp"]').forEach((card, index) => {
          const delay = parseFloat(card.dataset.delay) || 0;

          gsap.fromTo(card, {
            opacity: 0,
            y: 30
          }, {
            opacity: 1,
            y: 0,
            duration: 0.6,
            delay: delay,
            ease: "power2.out",
            scrollTrigger: {
              trigger: card,
              start: "top 85%",
              toggleActions: "play none none reverse"
            }
          });
        });
      } else {
        // En mobile, simplemente hacer visibles los elementos
        gsap.set('[data-gsap="fadeInUp"], [data-gsap="slideInUp"], .services-note', {
          opacity: 1,
          y: 0
        });
      }

      // 3D hover effect
      document.querySelectorAll('[data-hover-3d]').forEach(card => {
        card.addEventListener('mousemove', (e) => {
          const rect = card.getBoundingClientRect();
          const x = e.clientX - rect.left;
          const y = e.clientY - rect.top;
          const centerX = rect.width / 2;
          const centerY = rect.height / 2;
          const rotateX = (y - centerY) / centerY * -5;
          const rotateY = (x - centerX) / centerX * 5;

          gsap.to(card, {
            rotateX: rotateX,
            rotateY: rotateY,
            duration: 0.3,
            ease: "power2.out"
          });
        });

        card.addEventListener('mouseleave', () => {
          gsap.to(card, {
            rotateX: 0,
            rotateY: 0,
            duration: 0.5,
            ease: "power2.out"
          });
        });
      });

      // CTA button functionality
      document.querySelectorAll('.service-cta').forEach(button => {
        button.addEventListener('click', (e) => {
          const service = e.currentTarget.dataset.service;
          const message = `Hola, me interesa el servicio de ${service}. ¿Podrían enviarme más información?`;
          const whatsappUrl = `https://wa.me/34123456789?text=${encodeURIComponent(message)}`;
          window.open(whatsappUrl, '_blank');
        });
      });

      // Floating elements animation
      gsap.to('.float-element', {
        y: "+=20",
        rotation: "+=45",
        duration: gsap.utils.random(8, 15),
        ease: "sine.inOut",
        repeat: -1,
        yoyo: true,
        stagger: {
          each: 2,
          from: "random"
        }
      });
    };

    // Initialize when DOM is ready
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initDesignServices);
    } else {
      initDesignServices();
    }
  }
</script>